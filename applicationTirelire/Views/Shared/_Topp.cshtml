@model applicationTirelire.Models.TotListVM
@{
    ViewBag.Title = "_Topp";
}

    <div class="text-center">
        <h2>Top 5 des meilleurs produits</h2>
        @for (int i = 0; i < Model.ListIds.Count; i++)
        {

            <div class="col-lg-2">
                <div class="col-md-12">
                    @Html.DisplayFor(p => p.ListIds[i].NomProd)
                </div>
                <div>
                    <a href="~/Produit/Details/@Model.ListIds[i].IdProd">
                        <img src="@Url.Content("~/Images/" + Model.ListIds[i].IdProd + "_" + Model.ListIds[i].Url)" width="100" />
                    </a>
                </div>
                <div>
                    <span>Quantités vendues :</span>
                    @Html.DisplayFor(t => t.ListIds[i].Total)
                </div>

            </div>
        }

</div>
        @*<div class="carousel-inner">
            @for (int i = 0; i < Model.ListIds.Count; i++)
            {
                <div class="carousel-item active">

                    <div class="col-lg-2 d-block w-100">
                        <div class="carousel-caption d-none d-md-block">
                            <div class="col-md-12">
                                @Html.DisplayFor(p => p.ListIds[i].NomProd)
                            </div>
                            <div>
                                <a href="~/Produit/Details/@Model.ListIds[i].IdProd">
                                    <img src="@Url.Content("~/Images/" + Model.ListIds[i].IdProd + "_" + Model.ListIds[i].Url)" width="100" />
                                </a>
                            </div>
                            <div>
                                <span>Quantités vendues :</span>
                                @Html.DisplayFor(t => t.ListIds[i].Total)
                            </div>
                        </div>
                    </div>
                </div>
             }


         </div>
                <a class="carousel-control-prev" href="#carouselExampleCaptions" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleCaptions" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
        </div>
     </div>*@